# Relay backend host (hostname only, no scheme or path).
# Optional — falls back to ajax-relay.kb3tonline.com if unset.
# Copy this file to .env to override.
VITE_RELAY_HOST=ajax-relay.kb3tonline.com
